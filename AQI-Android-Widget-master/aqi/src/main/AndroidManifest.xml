<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tagadvance.aqi" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <receiver android:name=".AirQualityIndex" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/air_quality_index_info" />
        </receiver>

        <activity android:name=".AirQualityIndexConfigureActivity" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <activity android:name=" .AirNowAIP">
            </activity>

            <activity android:name=" .AirQualityIndex">
            </activity>

            <activity android:name=" .AirQualityIndexConfigureActivity">
            </activity>

            <activity android:name=" .AQI">
            </activity>

            <activity android:name=" .WidgetPreferences">
            </activity>

        </activity>


</application>

</manifest>
